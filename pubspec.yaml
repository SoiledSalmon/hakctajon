name: ai_loan_buddy
description: "AI-powered multilingual loan advisor application"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # -----------------------
  # Firebase Dependencies
  # -----------------------
  firebase_core: ^3.15.2
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.6.0
  firebase_storage: ^12.3.0

  # (Optional but recommended packages)
  google_fonts: ^6.2.1
  provider: ^6.1.2
  http: ^1.2.2
  flutter_riverpod: 2.6.1
  lottie: ^3.3.2
  flutter_svg: ^2.2.3
  uuid: ^4.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # Add assets here if needed
  # assets:
  #   - assets/images/
  #   - assets/icons/
